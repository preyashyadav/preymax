{
  "name": "preymax",
  "version": "0.1.0",
  "description": "Named, voice-summarized permission triage for parallel Claude Code sessions.",
  "type": "module",
  "bin": {
    "preymax": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "THREAT_MODEL.md",
    "docs"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "daemon": "node dist/cli.js daemon",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "license": "MIT",
  "private": true
}
